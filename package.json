{
  "name": "50-states-project",
  "proxy": "http://localhost:5000/",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "run-p start:form start:map start:server",
    "install": "run-p install:form install:map install:server",
    "start:form": "ENV=LOCAL_DEV cd suggestion-form && npm run start",
    "start:map": "ENV=LOCAL_DEV cd suggestion-map && npm run start",
    "start:server": "ENV=LOCAL_DEV cd server && npm run start",
    "install:form": "cd suggestion-form && npm ci",
    "install:map": "cd suggestion-map && npm ci",
    "install:server": "cd server && npm ci",
    "build:form": "cd suggestion-form && npm run build",
    "build:map": "cd suggestion-map && npm run build",
    "build": "run-p build:form build:map",
    "prebuild": "rimraf build && mkdir build",
    "move:js": "mv ./suggestion-form/build/static/js/main*.js ./build/form-bundle.js && mv ./suggestion-map/build/static/js/main*.js ./build/map-bundle.js",
    "move:css": "mv ./suggestion-form/build/static/css/main*.css ./build/form.css && mv ./suggestion-map/build/static/css/main*.css ./build/map.css",
    "move:html": "mv ./suggestion-form/build/index.html ./build/form.html && mv ./suggestion-map/build/index.html ./build/map.html",
    "move:for-server": "cp -r ./build server/assets",
    "build:gh-pages": "npm run build:form && cd suggestion-form && npm run build && cp ./build/static/js/*.js ../static && cp ./build/static/css/*.css ../static && cp ./build/index.html ../index.html",
    "postbuild": "npm run move:js && npm run move:css && npm run move:html && npm run move:for-server && ECHO \"*\n*\n*\n* 🚀  Now you'll need to update the links in the html file to point to the right filenames\n*\n*\n*\"",
    "deploy-server": "cd server && sls deploy",
    "teardown-server": "cd server && sls remove"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "rimraf": "^3.0.0"
  }
}